# Cedar Gate Technologies - EZNet Uses Hardcoded Encryption Key for ADFS Passwords

## Vendors

* Cedar Gate Technologies

## Affected Products

This vulnerability was confirmed on EZ-NET Portal 6.9.0.

## Summary

The use of reversible encryption in EZ-NET Portal 6.9.0 allows an attacker with access to encrypted Active Directory Federation Services (ADFS) passwords to recover the plaintext values via a hardcoded key.

## Remediation/Mitigation

Upgrade to the latest version of the product. This vulnerability was remediated in the September 2025 Cumulative Release (CR).

## Credit

This issue was found by Justin Kennedy and Stephen Breen of Atredis Partners.

## References

* https://cwe.mitre.org/data/definitions/321.html

## Report Timeline

* 2026-02-04: Atredis Partners sent an initial notification to vendor via contact form
* 2026-02-05: Atredis Partners sent initial notification via email
* 2026-02-06: Vendor confirms interest and requests more details about the vulnerability
* 2026-02-06: Atredis Partners proposes secure communication channels
* 2026-02-06: Vendor sends invite for secure upload
* 2026-02-06: Atredis Partners uploads technical details to secure share
* 2026-02-06: Atredis Partners notifies vendor of public disclosure process
* 2026-02-06: Vendor Head of Cybersecurity calls to discuss vulnerability and notes it was fixed in a recent release
* 2026-02-06: Vendor requests full URL and investigation trigger
* 2026-02-06: Atredis Partners asks for the specific release version where the fix was applied
* 2026-02-11: Atredis Partners follows up on release version and CVE status
* 2026-02-11: Vendor confirms vulnerability was remediated in September 2025 release
* 2026-02-12: Atredis Partners asks for security advisory, CVE status, fix details, and discusses disclosure timeline
* 2026-02-23: Atredis Partners sends final check-in before obtaining CVE and moving to public disclosure
* 2026-02-23: Vendor confirms remediation date and gives approval to proceed with disclosure

## Technical Details

The EZ-NET Portal is storing Active Directory Federation Services (ADFS) passwords in an encrypted format in the backend database using a key hardcoded in the application source code:

```csharp
public string ADFS_Encrypt(string clearText)
{
    byte[] bytes = Encoding.Unicode.GetBytes(clearText);
    using (Aes aes = Aes.Create())
    {
        Rfc2898DeriveBytes rfc2898DeriveBytes = new Rfc2898DeriveBytes(this.PhraseKey, new byte[]
        {
            73,
            118,
            97,
            110,
            32,
            77,
            101,
            100,
            118,
            101,
            100,
            101,
            118
        });
        aes.Key = rfc2898DeriveBytes.GetBytes(32);
        aes.IV = rfc2898DeriveBytes.GetBytes(16);
        using (MemoryStream memoryStream = new MemoryStream())
        {
            using (CryptoStream cryptoStream = new CryptoStream(memoryStream, aes.CreateEncryptor(), CryptoStreamMode.Write))
            {
                cryptoStream.Write(bytes, 0, bytes.Length);
                cryptoStream.Close();
            }
            clearText = Convert.ToBase64String(memoryStream.ToArray());
        }
    }
    return clearText;
}

private string PhraseKey
{
    get { return "<REDACTED>"; }
}
```
`CedarGate/eznet-decomp/CedarGate.Eznet.Business/EncdeclookUpProvider.cs`

An attacker with knowledge of the hardcoded encryption key and the encrypted password values can decrypt the passwords to their original plaintext values.
